<UserControl
    x:Class="ComfyCarry.Views.ArtifactListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ComfyCarry.Views">
    <Grid>
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
            <ItemsRepeater x:Name="Repeater">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="120" MinItemHeight="120" MinRowSpacing="8" MinColumnSpacing="8"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:RemoteEntryVM">
                        <StackPanel Width="120" Spacing="4">
                            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="4" Height="100">
                                <Image Source="{x:Bind Thumb}" Stretch="UniformToFill" VerticalAlignment="Center"/>
                            </Border>
                            <TextBlock Text="{x:Bind Name}" TextTrimming="CharacterEllipsis" FontSize="11"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
        <ProgressRing x:Name="Busy" IsActive="False" Width="32" Height="32" VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
